# Scripts

Deterministic generation, metadata export, validation, Merkle-tree generation, deployment helpers and release-audit scripts belong here when their corresponding phases begin.

Any script that changes canonical production data must be reproducible, reviewable and produce auditable output/hashes where appropriate.
