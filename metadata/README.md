# Metadata

Metadata production workspace.

Final metadata must be generated from the same frozen manifest used for final artwork rendering.

Current unresolved items include:
- exact schema / attribute names;
- collection description;
- Market Genome final representation;
- image CID;
- metadata CID.

Do not publish a final metadata package until V6B artwork/manifest QA and the relevant decision gates pass.
