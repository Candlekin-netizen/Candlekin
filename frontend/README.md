# Frontend

Production frontend workspace.

Do not implement against prototype mock state. The production frontend must use centralized chain/contract configuration, real wallet state, fresh safety-critical contract reads, receipt-verified transactions, real ownership data, and canonical tokenURI/IPFS metadata.

See `docs/FRONTEND_EXPERIENCE_BRIEF.md` before implementation.
